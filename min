const noise = new SimplexNoise();

for (let x = 0; x < width; x++) {
  for (let z = 0; z < depth; z++) {
    const height = noise.noise2D(x / 100, z / 100) * 10 + 5;
    for (let y = 0; y < height; y++) {
      const block = createBlock(x, y, z);
      scene.add(block);
    }
  }
}
